version: "3.8"

services:

  postgres:
    image: postgres:16

    environment:
      POSTGRES_USER: arena
      POSTGRES_PASSWORD: arena
      POSTGRES_DB: codearena

    ports:
      - "5432:5432"

    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres: